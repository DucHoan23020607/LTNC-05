#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int n;
    cin >> n;
    vector<int> ar(n);
    //sort(ar.begin(), ar.end());
    for(int i = 0; i < n; i++){
            cin >> ar[i];
    }
                    sort(ar.begin(), ar.end());
        for(int i = 0; i < n; i++){
            cout << ar[i] << " ";
    }
    return 0;
}
